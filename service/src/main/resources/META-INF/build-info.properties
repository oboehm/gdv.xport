#
#   Build-Properties, die waehrend des Maven-Builds durch die
#   aktuellen Werte aus dem POM befuellt werden. Und vom
#   spring-boot-maven-plugin angereichert werden.
#
#   (c)reated: 16-Mai-2017 by Oli B.
#

# see http://docs.spring.io/spring-boot/docs/current/maven-plugin/examples/build-info.html

# see https://tedvinke.wordpress.com/2013/04/20/using-jenkins-build-version-with-maven/
build.number=${build.number}
build.id=${build.id}
build.branch=${git.branch}

# POM Settings
project.version=${project.version}
project.groupId=${project.groupId}
project.artifactId=${project.artifactId}
